from p0 to p

### images
an imaging device gather the light reflected by 3d objects to create a 2d representation of the scene
- image formation and acquisition process:
	- the geometric relationship between scene points and image points
		- scene points: real fisical points
		- image points: the representations
	- the radiometric relationship between the brightness of image points and the light emitted by scene points
	- image digitalization process

to device such hardware the simplest model is known as the pinhole camera model
we are constructing a methematical framework that transforms a point in 3d to a number in a image.
Geometrically, the image is achueved by drawing straight rays from the scene points through the hole up to the image


### prespective projection
the geometric model of the image formation in a pinhole is known as prespective projection
C is the piercing point
we are looking for the mathematical relation between the two points
3d point capital 2d point small

![[Pasted image 20260924152251.png]]

![[Pasted image 20260924152648.png]]
with proportianl triangles when can derives these formulas
![[Pasted image 20260924153144.png]]

- image coordinates are a scaled version of the scene coordinates(function of depth)
	- if z increases the farther the point the smaller the coordinates (object distant from a camera)
	- the larger the focal length the bigger the object in the image (and vice versa)

the image formation process deals with mapping a 3d space onto a 2d this entails loosing information of depth, while we can map a 3d point to a 2d point we can only map a 2d point to a ray because whe lost the information of depth during mapping

![[Pasted image 20260924154623.png]]

### stand
the solution is to use multiple images the huma vision is a stereo vision system, stereo images allow to infer ed
the only problem is that we have to know the correspondences between the points in the 2 planes so that 3d information can be recovered easily by triangulation

![[Pasted image 20260924155301.png]]

in standard stereo geometry we make some assumption:
- the axes of the two camera are parralels
- and they have the same focal lenght so the images are coplanar
The transformation between the two
reference frames is just a translation (b),
usually horizontal

![[Pasted image 20260924155905.png]]

the two cameras are displaced at a given quantity *b* called baseline
Disparity: difference between the horizontal coordinates in the left and right images (horizontal displacement)
Inverse relation: the larger the disparity the smaller depth, and vice versa
![[Pasted image 20260924160139.png]]

